apiVersion: v1
kind: Service
metadata:
  name: tsangwu-server
  labels:
    app: tsangwu-server
spec:
  selector:
    app: tsangwu-server
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: grpc
      port: 9090
      targetPort: grpc
  type: ClusterIP
